<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/ll_to_do_list_plane"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/color_panel_background"
    android:orientation="vertical"
    android:layout_marginVertical="4dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center|left"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="8dp">

        <ImageButton
            android:id="@+id/ib_title_handle_to_do_list"
            android:layout_width="wrap_content"
            android:minWidth="2dp"
            android:layout_height="18dp"
            android:background="@color/color_on_top_primary_blue"
            />

        <TextView
            android:id="@+id/tv_to_do_list_plane_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text="To Do"
            android:textColor="?colorOnBackground"
            android:textSize="18sp"
            android:textStyle="bold"/>
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_task_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:nestedScrollingEnabled="false"
        android:elevation="1dp"
        android:paddingHorizontal="8dp"
        android:overScrollMode="never"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

</LinearLayout>